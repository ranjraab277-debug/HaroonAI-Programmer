# Haroon AI account setup

This version does not use Roblox OAuth. Users create a Haroon AI account with a username and password. Roblox OAuth client ID/secret are not required.

The Roblox Lite Bridge still uses `AGENT_TOKEN` to authenticate with the backend.
